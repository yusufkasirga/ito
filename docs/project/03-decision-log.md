# Decision Log — Itinerary of Türkiye
**Document:** /docs/project/03-decision-log.md
**Last updated:** 2026-07-03

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-01 | 2026-07-03 | WhatsApp number moved to `NEXT_PUBLIC_WHATSAPP_E164_NUMBER` env var; all 7 hardcoded occurrences (6 in `app/page.tsx`, 1 in `app/layout.tsx` JSON-LD) replaced with `whatsAppUrl()` from `lib/config.ts`. | Owner directive: number must be environment-configured, never hardcoded. Single source of truth simplifies future number changes. | ✅ Implemented |
| D-02 | 2026-07-03 | **Removed** the `/api/lead` server route (added in the previous iteration) and the associated webhook forwarding. The application form now builds a structured message in the browser and opens WhatsApp — nothing is stored server-side. | Owner directive: "Do not store form data, medical records, … sensitive health information." WhatsApp-only intake also matches the existing Kommo + 360dialog operational stack, so leads still land in the CRM via the WhatsApp Business channel. | ✅ Implemented |
| D-03 | 2026-07-03 | Removed the fabricated "N clients helped right now" counter (random number generator) from the hero trust strip; replaced with "Replies within hours — real people, no bots". | A simulated live counter is a fabricated trust signal. If discovered it damages credibility and conflicts with the "no unsupported claims" content rule. The replacement claim is operationally truthful and consistent with the "How it works" copy. | ✅ Implemented |
| D-04 | 2026-07-03 | Sensitive-data guidance added in three places: beside the form, on the success screen, and inside the prefilled WhatsApp message itself. | Owner directive. Embedding the reminder in the message itself means the guidance survives even if the user skips reading the page. | ✅ Implemented |
| D-05 | 2026-07-03 | Future Services pages were **not created**. Footer link deferred. | No approved Future Services copy exists in the repository. Creating pages would require writing new content, violating the "approved repository copy was not rewritten / no unsupported claims" rule. Escalated in the content-conflicts document. | ⏸ Awaiting owner copy |
| D-06 | 2026-07-03 | Site positioning left as-is (four equal pillars) rather than restructured around "Medical Travel + Private Türkiye Experiences primary". | Repositioning requires rewriting approved homepage copy, nav structure, and the services page — a strategy change, not a QA fix. Escalated for owner approval. | ⏸ Awaiting owner decision |
| D-07 | 2026-07-03 | Existing quantitative claims ("1,200+ successful procedures", "98% Satisfaction" badges, "Verified Client" labels with stock-photo avatars, "Available 24/7") left in place but flagged. | These are part of the approved repository copy; removing them unilaterally would violate the content rule. They are unsupported by any source in the repo, so they are escalated rather than silently kept or deleted. | ⏸ Awaiting owner evidence or removal approval |
| D-08 | 2026-07-03 | Accessibility layer added (reduced-motion, focus-visible, keyboard dropdown, ARIA on accordions/carousel) without altering visual design. | QA requirement; zero visual impact for mouse users, material improvement for keyboard/AT users. | ✅ Implemented |
| D-09 | 2026-07-03 | Kept blog rendering fully static (SSG via `generateStaticParams`); no CMS introduced. | Three posts live in `blogData.ts`; a CMS is premature. The Cowork blog-automation kit can append to this file programmatically. | ✅ Implemented |
| D-10 | 2026-07-03 | Domain remains `ito-rust.vercel.app` in all canonical URLs, sitemap and JSON-LD, centralised as `SITE_URL` in `lib/config.ts` and constants in `app/sitemap.ts` / blog metadata. | No production domain has been approved or configured. When one is, it must be changed in `lib/config.ts`, `app/sitemap.ts`, `app/robots.ts`, `app/layout.tsx`, blog `[slug]` metadata and the four route layouts (single search-and-replace of the URL). | ⏸ Awaiting domain decision |
