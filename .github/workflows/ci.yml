name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint changed files
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          files="$(git diff --name-only --diff-filter=ACMRT "origin/${{ github.base_ref }}"...HEAD -- '*.js' '*.jsx' '*.mjs' '*.ts' '*.tsx')"
          if [ -n "$files" ]; then
            npx eslint $files
          fi
      - name: Build
        run: npm run build
